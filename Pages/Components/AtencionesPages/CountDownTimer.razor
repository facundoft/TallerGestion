<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS Only Circle Countdown Timer</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #282c34;
            font-family: 'Arial', sans-serif;
            margin: 0;
        }

        .circle-container {
            position: relative;
            width: 200px;
            height: 200px;
        }

        .circle {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: none;
            position: relative;
        }

        .progress {
            position: absolute;
            top: 0;
            left: 0;
            transform: rotate(-90deg);
        }

        .progress circle {
            stroke-dasharray: 314;
            stroke-dashoffset: 314;
            animation: fillCircle 10s linear forwards;
            filter: drop-shadow(0 0 10px #00f9ff);
        }

        .counter::after {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 2em;
            color: white;
            animation: countdown 10s steps(10, end) forwards;
            content: "10";
        }

        @@keyframes fillCircle {
            100% {
                stroke-dashoffset: 0;
            }
        }

        @@keyframes countdown {
            0% { content: "10"; }
            10% { content: "9"; }
            20% { content: "8"; }
            30% { content: "7"; }
            40% { content: "6"; }
            50% { content: "5"; }
            60% { content: "4"; }
            70% { content: "3"; }
            80% { content: "2"; }
            90% { content: "1"; }
            100% { content: "0"; }
        }
    </style>
</head>
<body>
    <div class="circle-container">
        <div class="circle">
            <svg class="progress" width="200" height="200">
                <defs>
                    <linearGradient id="gradient" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" style="stop-color:#00f9ff; stop-opacity:1" />
                        <stop offset="100%" style="stop-color:#0041ff; stop-opacity:1" />
                    </linearGradient>
                </defs>
                <circle cx="100" cy="100" r="50" stroke-width="8" fill="none" 
                        stroke="url(#gradient)"></circle>
            </svg>
            <div class="counter"></div>
        </div>
    </div>
</body>
</html>